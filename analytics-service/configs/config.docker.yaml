grpc:
  addr: ":8081"

postgres:
  url: ""  # overridden by ANALYTICS_DATABASE_URL

movie_collection:
  addr: "movie-service:50052"
  timeout: 3s

recommendation:
  default_limit: 10
  candidate_pool_size: 100

open_router:
  api_key: ""  # set ANALYTICS_OPENROUTER_API_KEY to enable
  model: "openrouter/free"
  timeout: 60s

shutdown_timeout: 10s
